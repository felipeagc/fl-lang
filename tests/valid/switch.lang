module main

import fmt "core:fmt"
import "core:c"

func main() {
    switch (4) {
    case 1: abort();
    case 2: abort();
    default: fmt.print("None of the above!\n");
    }

    switch (1) {
    case 1:
    default: abort();
    }

    switch (1) {
    default: 
        break;
        abort();
    }
}
