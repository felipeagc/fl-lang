extern fn main() -> i32 {
    puts_fn(c"Hello");

    printf(c"other_int: %d\n", other_int);

    const other_int = some_int;

    return 0;
}

import "core:c";

const some_int: i32 = 123;
const puts_fn = puts;
