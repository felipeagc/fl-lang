import hello "imported.lang";
import c "core:c";

proc main() void {
    hello.say_hello();
    var a: i32 = hello.some_const;
    var b: i32 = hello.some_var;

    var some_var: hello.SomeType;
    some_var.field = 32;

    c.printf(c"Hello\n");
}
