import c "core:c";

proc "c" main() void {
    var test: i32 = 321;

    if (test) c.printf(c"Hello!\n");

    if (test || false) {
        c.printf(c"Hello!\n");
    } else {
        c.printf(c"No: %d!\n", constant);
        const constant: i32 = 9;
    }

    c.printf(c"Yo: %d!\n", test);

    {
        var test2: i32 = 456;
        c.printf(c"Yo: %d!\n", test);
        c.printf(c"Yo: %d!\n", test);
        c.printf(c"Yo: %d!\n", test2);
        c.printf(c"Yo: %d!\n", constant);
        const constant: i32 = 534;
    }

    if (false || false) c.abort();
    if (true && false) c.abort();
    if (false && false) c.abort();
    if (false && true) c.abort();
    if (!(true && true)) c.abort();

    var n: i32 = 0;
    while (n < 5) {
        c.printf(c"%d\n", n);
        n = n + 1;
        break;
    }
    c.printf(c"n = %d\n", n);
    
    for (var i: i32 = 0; i < 10; i += 1) {
        c.printf(c"i = %d\n", i);
        if (!(i < 10)) c.abort();
    }

    n = 0;
    for (;;) {
        c.printf(c"Yo\n");
        n += 1;
        if (n < 5) {
            continue;
        }
        break;
    }
}
