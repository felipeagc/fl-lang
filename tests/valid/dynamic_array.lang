import "core:c";
import "core:array";

extern fn main() -> i32 {
    var arr: [dyn]i32;
    arr.ptr = null;
    arr.len = 0;
    arr.cap = 0;

    array_push!(i32)(&arr, 1);
    array_push!(i32)(&arr, 2);

    if (arr.len != 2) abort();

    array_free!(i32)(&arr);

    return 0;
}
